import React from "react";

export const HelloWorld = () => <h1>I am from HelloWorld Component </h1>;
// if export with function than name cannot be changed when importing

// export default HelloWorld
// If export Default exists than name can be changed When Importing
